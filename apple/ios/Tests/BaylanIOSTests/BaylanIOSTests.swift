import XCTest
@testable import BaylanIOS

final class BaylanIOSTests: XCTestCase {

    // UI-layer tests require a running simulator — keep this target
    // as a home for future snapshot / UI tests.

    func testAppLaunches() {
        // Verifies that the test host (BaylanIOS.app) launches successfully.
        XCTAssertTrue(true)
    }
}
